using System;
using System.IO;
using posLibrary;

namespace pos
{
    class Program
    {
        static void Main(string[] args) { }
        //{
        //    // Create database
        //    var dbConnection = new DataBaseConnection();
        //    dbConnection.CreateDatabase();

        //    // Create a user (e.g., Cashier)
        //    var cashier = new Cashier(2, "cashier1");

        //    // Create a cart for the cashier
        //    var cart = new Cart();

        //    // Create categories and products
        //    var category = new Categories(1, "Category1");
        //    var product1 = new Products(1, "Product1", 100.00m, 10, category, 123456);
        //    var product2 = new Products(2, "Product2", 200.00m, 20, category, 789012);

        //    // Add items to the cart
        //    cart.AddItem(product1);
        //    cart.AddItem(product1); // Should increase quantity
        //    cart.AddItem(product2);

        //    // Print receipt
        //    cart.PrintReceipt();

        //    // Save to database
        //    string dbPath = Path.Combine(Directory.GetCurrentDirectory(), "pos.db");
        //    cart.SaveToDatabase(dbPath);
        //}
    }
}